# WDS Form Components

> **Category**: 폼 입력 컴포넌트
> **Parent**: [DESIGN_TOKENS.md](./DESIGN_TOKENS.md)
> **Validation**: react-hook-form + zod
> **Last Updated**: 2026-01-16

---

## 1. FormField (폼 필드 래퍼)

레이블, 입력, 에러 메시지를 포함하는 폼 필드 래퍼입니다.

### Props Interface

```tsx
interface FormFieldProps {
  label: string;
  name: string;
  required?: boolean;
  error?: string;
  helper?: string;
  children: React.ReactNode;
}
```

### Layout

```
┌─────────────────────────────────────────────────────────────┐
│  모임 이름 *                                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  책벌레들                                            │   │
│  └─────────────────────────────────────────────────────┘   │
│  모임 이름은 2~20자 이내로 입력해주세요                       │
└─────────────────────────────────────────────────────────────┘

[에러 상태]
┌─────────────────────────────────────────────────────────────┐
│  모임 이름 *                                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  책                                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│  ⚠️ 최소 2자 이상 입력해주세요                              │
└─────────────────────────────────────────────────────────────┘
```

### Styling

| Property | Value |
|----------|-------|
| Label Font | `typography.w6` |
| Label Color | `colors.textPrimary` |
| Required Mark | `colors.error` |
| Helper Font | `typography.w7` |
| Helper Color | `colors.textSecondary` |
| Error Color | `colors.error` |
| Gap | 8px |

### Usage

```tsx
<FormField
  label="모임 이름"
  name="challengeName"
  required
  error={errors.challengeName?.message}
  helper="모임 이름은 2~20자 이내로 입력해주세요"
>
  <Input {...register('challengeName')} />
</FormField>
```

---

## 2. AmountInput (금액 입력)

금액 입력 전용 컴포넌트입니다. 천 단위 콤마 자동 포맷팅을 지원합니다.

### Props Interface

```tsx
interface AmountInputProps {
  value: number | undefined;
  onChange: (value: number | undefined) => void;
  min?: number;
  max?: number;
  step?: number;              // 기본: 1000
  suffix?: string;            // 기본: '원'
  placeholder?: string;
  disabled?: boolean;
  error?: string;
  presets?: number[];         // 빠른 선택 버튼
}
```

### Layout

```
┌─────────────────────────────────────────────────────────────┐
│  월 회비                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  50,000                                         원  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  [+10,000]  [+50,000]  [+100,000]                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Features

| 기능 | 설명 |
|------|------|
| 자동 포맷팅 | 1000 → 1,000 |
| 숫자만 입력 | 비숫자 문자 자동 필터 |
| Preset 버튼 | 빠른 금액 선택 |
| Min/Max 검증 | 범위 초과 시 에러 |
| Tabular Numbers | `font-feature-settings: "tnum"` |

### Styling

| Property | Value |
|----------|-------|
| Font | `typography.financialMedium` |
| Text Align | right |
| Suffix Color | `colors.textSecondary` |
| Preset Button | `colors.grey100` 배경 |

### Usage

```tsx
<AmountInput
  value={monthlyFee}
  onChange={setMonthlyFee}
  min={10000}
  max={1000000}
  step={10000}
  suffix="원"
  placeholder="0"
  presets={[10000, 50000, 100000]}
/>
```

---

## 3. Select (드롭다운 선택)

옵션 선택 드롭다운 컴포넌트입니다.

### Props Interface

```tsx
interface SelectProps {
  value: string | undefined;
  onChange: (value: string) => void;
  options: SelectOption[];
  placeholder?: string;
  disabled?: boolean;
  error?: string;
  searchable?: boolean;       // 검색 가능
}

interface SelectOption {
  value: string;
  label: string;
  icon?: React.ReactNode;
  disabled?: boolean;
}
```

### Layout

```
[닫힘 상태]
┌─────────────────────────────────────────────────────────────┐
│  카테고리를 선택하세요                                  ▼   │
└─────────────────────────────────────────────────────────────┘

[열림 상태]
┌─────────────────────────────────────────────────────────────┐
│  카테고리를 선택하세요                                  ▲   │
├─────────────────────────────────────────────────────────────┤
│  🏢 장소 대관                                               │
│  🍽️ 식비                                                   │
│  📦 물품 구매                                               │
│  📋 기타                                                    │
└─────────────────────────────────────────────────────────────┘
```

### Styling

| Property | Value |
|----------|-------|
| Height | 48px |
| Background | White |
| Border | 1px solid `colors.border` |
| Border Radius | `shape.radiusMd` |
| Focus Border | `colors.orange500` |
| Option Hover | `colors.grey100` |
| Selected | `colors.orange100` |

### Usage

```tsx
<Select
  value={category}
  onChange={setCategory}
  placeholder="카테고리를 선택하세요"
  options={[
    { value: 'MEETING', label: '장소 대관', icon: '🏢' },
    { value: 'FOOD', label: '식비', icon: '🍽️' },
    { value: 'SUPPLIES', label: '물품 구매', icon: '📦' },
    { value: 'OTHER', label: '기타', icon: '📋' },
  ]}
/>
```

---

## 4. TextArea (다중 줄 입력)

여러 줄 텍스트 입력 컴포넌트입니다.

### Props Interface

```tsx
interface TextAreaProps {
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  rows?: number;              // 기본: 4
  maxLength?: number;
  showCount?: boolean;        // 글자 수 표시
  disabled?: boolean;
  error?: string;
}
```

### Layout

```
┌─────────────────────────────────────────────────────────────┐
│  이번 모임에서 읽은 책에 대한 감상을 자유롭게                │
│  작성해주세요.                                              │
│                                                             │
│                                                             │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                                               45 / 500      │
└─────────────────────────────────────────────────────────────┘
```

### Styling

| Property | Value |
|----------|-------|
| Min Height | 120px (4줄 기준) |
| Padding | 12px |
| Font | `typography.w5` |
| Counter Font | `typography.w7` |
| Counter Color | `colors.textSecondary` |

### Usage

```tsx
<TextArea
  value={content}
  onChange={setContent}
  placeholder="이번 모임에서 읽은 책에 대한 감상을 자유롭게 작성해주세요."
  maxLength={500}
  showCount
  rows={6}
/>
```

---

## 5. ImageUpload (이미지 업로드)

이미지 업로드 및 미리보기 컴포넌트입니다.

### Props Interface

```tsx
interface ImageUploadProps {
  value: File | string | undefined;  // File 또는 URL
  onChange: (file: File | undefined) => void;
  accept?: string;            // 기본: 'image/*'
  maxSize?: number;           // bytes (기본: 5MB)
  aspectRatio?: number;       // 비율 (예: 16/9)
  placeholder?: string;
  disabled?: boolean;
  error?: string;
}
```

### Layout

```
[빈 상태]
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                         📷                                  │
│                                                             │
│              챌린지 대표 이미지를 등록하세요                  │
│              JPG, PNG (최대 5MB)                            │
│                                                             │
│              ┌────────────────┐                             │
│              │   이미지 선택   │                             │
│              └────────────────┘                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘

[업로드 후]
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   ┌─────────────────────────────────────────────────────┐  │
│   │                                                      │  │
│   │               [이미지 미리보기]                       │  │
│   │                                                      │  │
│   └─────────────────────────────────────────────────────┘  │
│                                                             │
│   [변경]  [삭제]                                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### States

| State | 표시 |
|-------|------|
| Empty | 업로드 가이드 |
| Uploading | Spinner + Progress |
| Uploaded | 미리보기 + 변경/삭제 버튼 |
| Error | 에러 메시지 + 재시도 |

### Styling

| Property | Value |
|----------|-------|
| Border | 2px dashed `colors.border` |
| Border Radius | `shape.radiusLg` |
| Background | `colors.grey50` |
| Drag Active | Border `colors.orange500` |
| Padding | 24px |

### Usage

```tsx
<ImageUpload
  value={coverImage}
  onChange={setCoverImage}
  maxSize={5 * 1024 * 1024}
  aspectRatio={16 / 9}
  placeholder="챌린지 대표 이미지를 등록하세요"
/>
```

---

## 6. CheckboxGroup (체크박스 그룹)

다중 선택 체크박스 그룹입니다.

### Props Interface

```tsx
interface CheckboxGroupProps {
  value: string[];
  onChange: (value: string[]) => void;
  options: CheckboxOption[];
  direction?: 'horizontal' | 'vertical';
  disabled?: boolean;
}

interface CheckboxOption {
  value: string;
  label: string;
  description?: string;
  disabled?: boolean;
}
```

### Layout

```
┌─────────────────────────────────────────────────────────────┐
│  알림 설정                                                  │
│                                                             │
│  ☑️ 투표 알림                                               │
│     새로운 투표가 생성되면 알려드려요                         │
│                                                             │
│  ☑️ 납입 알림                                               │
│     납입일 3일 전에 알려드려요                               │
│                                                             │
│  ☐ 게시글 알림                                              │
│     새 게시글이 올라오면 알려드려요                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Usage

```tsx
<CheckboxGroup
  value={notifications}
  onChange={setNotifications}
  options={[
    { value: 'vote', label: '투표 알림', description: '새로운 투표가 생성되면 알려드려요' },
    { value: 'payment', label: '납입 알림', description: '납입일 3일 전에 알려드려요' },
    { value: 'post', label: '게시글 알림', description: '새 게시글이 올라오면 알려드려요' },
  ]}
  direction="vertical"
/>
```

---

## 7. Form (폼 컨테이너)

react-hook-form과 통합된 폼 컨테이너입니다.

### Props Interface

```tsx
interface FormProps<T extends FieldValues> {
  form: UseFormReturn<T>;
  onSubmit: (data: T) => void | Promise<void>;
  children: React.ReactNode;
  disabled?: boolean;
}
```

### Usage with Zod

```tsx
// Schema 정의
const createChallengeSchema = z.object({
  name: z.string().min(2, '최소 2자 이상').max(20, '최대 20자'),
  description: z.string().max(500, '최대 500자'),
  monthlyFee: z.number().min(10000, '최소 10,000원').max(1000000, '최대 1,000,000원'),
  category: z.enum(['READING', 'STUDY', 'EXERCISE', 'OTHER']),
  coverImage: z.instanceof(File).optional(),
});

type CreateChallengeForm = z.infer<typeof createChallengeSchema>;

// 컴포넌트에서 사용
function CreateChallengePage() {
  const form = useForm<CreateChallengeForm>({
    resolver: zodResolver(createChallengeSchema),
    defaultValues: {
      name: '',
      description: '',
      monthlyFee: 50000,
    },
  });

  const onSubmit = async (data: CreateChallengeForm) => {
    await api.createChallenge(data);
  };

  return (
    <Form form={form} onSubmit={onSubmit}>
      <FormField label="모임 이름" name="name" required>
        <Input {...form.register('name')} />
      </FormField>
      
      <FormField label="월 회비" name="monthlyFee" required>
        <AmountInput
          value={form.watch('monthlyFee')}
          onChange={(v) => form.setValue('monthlyFee', v ?? 0)}
          presets={[30000, 50000, 100000]}
        />
      </FormField>
      
      <Button type="submit" disabled={form.formState.isSubmitting}>
        모임 만들기
      </Button>
    </Form>
  );
}
```

---

## 관련 문서

- [DESIGN_TOKENS.md](./DESIGN_TOKENS.md) - 메인 디자인 토큰
- [WDS_FOUNDATION.md](./WDS_FOUNDATION.md) - Input 컴포넌트
- [WDS_CALENDAR.md](./WDS_CALENDAR.md) - DatePicker 컴포넌트
- [FRONTEND_TECH_STACK.md](../FRONTEND_TECH_STACK.md) - react-hook-form + zod 설정
