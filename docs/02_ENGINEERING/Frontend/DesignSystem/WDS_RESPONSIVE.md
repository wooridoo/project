# WDS Responsive Policy

> **Category**: 해상도 정책 및 레이아웃 패턴
> **Parent**: [DESIGN_TOKENS.md](./DESIGN_TOKENS.md)
> **Philosophy**: 토스 스타일 3계층 구조 (논리/에셋/컴포넌트)
> **Last Updated**: 2026-01-16

---

## 1. 논리 해상도 (Logical Resolution)

### 1.1 Breakpoints

```css
/* tokens.css */
:root {
  --breakpoint-mobile: 375px;   /* 기준점 */
  --breakpoint-tablet: 768px;
  --breakpoint-desktop: 1280px;
  --breakpoint-wide: 1920px;
}
```

### 1.2 Media Queries

| 이름 | 범위 | 레이아웃 | 컴포넌트 변화 |
|------|------|----------|--------------|
| **Mobile** | 0-767px | 1 Column | 기본 (Mobile-First) |
| **Tablet** | 768-1279px | 2 Column | BottomNav → SideNav |
| **Desktop** | 1280-1919px | 3 Column | 풀 대시보드 |
| **Wide** | 1920px+ | 3 Column (중앙 정렬) | 최대 너비 제한 |

### 1.3 CSS 변수 활용

```css
/* 컨테이너 최대 너비 */
:root {
  --container-mobile: 100%;
  --container-tablet: 720px;
  --container-desktop: 1200px;
  --container-wide: 1400px;
}

/* 콘텐츠 영역 */
.content-container {
  width: 100%;
  max-width: var(--container-desktop);
  margin: 0 auto;
  padding: 0 16px;
}

@media (min-width: 768px) {
  .content-container {
    padding: 0 24px;
  }
}

@media (min-width: 1280px) {
  .content-container {
    padding: 0 32px;
  }
}
```

---

## 2. 에셋 해상도 (Asset Resolution)

### 2.1 이미지 밀도

| 밀도 | 배율 | 접미사 | 용도 | 디바이스 |
|------|------|--------|------|----------|
| Standard | 1x | `.png` | 저해상도 | 구형 데스크탑 |
| Retina | 2x | `@2x.png` | 고해상도 | 대부분의 디바이스 |
| Super Retina | 3x | `@3x.png` | 초고해상도 | iPhone Pro, 고급 Android |

### 2.2 srcset 패턴

```tsx
// 이미지 컴포넌트
interface ResponsiveImageProps {
  src: string;            // 기본 (1x)
  srcSet?: string;        // "image@2x.png 2x, image@3x.png 3x"
  alt: string;
  loading?: 'lazy' | 'eager';
}

// 사용 예
<ResponsiveImage
  src="/images/logo.png"
  srcSet="/images/logo@2x.png 2x, /images/logo@3x.png 3x"
  alt="WooriDo Logo"
/>
```

### 2.3 아이콘 규격

| 용도 | 1x | 2x | 3x |
|------|-----|-----|-----|
| App Icon | 24px | 48px | 72px |
| Tab Bar Icon | 24px | 48px | 72px |
| Feature Icon | 32px | 64px | 96px |
| Avatar (MD) | 40px | 80px | 120px |

---

## 3. 컴포넌트 규격 (Component Specs)

### 3.1 터치 타겟

| 규격 | 값 | 근거 |
|------|-----|------|
| **최소 터치 영역** | 44px × 44px | Apple HIG |
| **권장 터치 영역** | 48px × 48px | Google Material |
| **버튼 높이 (SM)** | 32px | 터치 패딩 포함 44px |
| **버튼 높이 (MD)** | 40px | 최적 터치 영역 |
| **버튼 높이 (LG)** | 48px | 네이티브 앱 느낌 |

### 3.2 간격 시스템 (Spacing)

| Token | Value | 용도 |
|-------|-------|------|
| `--space-0` | 0px | - |
| `--space-1` | 4px | 인라인 간격 |
| `--space-2` | 8px | 텍스트 간격, 아이콘 패딩 |
| `--space-3` | 12px | 카드 내부 패딩 |
| `--space-4` | 16px | 섹션 간격 |
| `--space-5` | 20px | 카드 패딩 |
| `--space-6` | 24px | 섹션 구분 |
| `--space-8` | 32px | 페이지 여백 |
| `--space-10` | 40px | 대형 섹션 구분 |
| `--space-12` | 48px | 페이지 간격 |

### 3.3 콘텐츠 최대 너비

| 유형 | 최대 너비 | 용도 |
|------|----------|------|
| 본문 텍스트 | 65ch (~600px) | 가독성 최적 |
| 카드 그리드 | 1200px | 데스크탑 레이아웃 |
| 모달 (SM) | 320px | 확인 다이얼로그 |
| 모달 (MD) | 480px | 일반 폼 |
| 모달 (LG) | 640px | 상세 정보 |

---

## 4. 레이아웃 패턴 (Layout Patterns)

### 4.1 페이지 레이아웃

#### Mobile Layout (< 768px)

```
┌─────────────────────────────────┐
│           Header (56px)         │
├─────────────────────────────────┤
│                                 │
│         Main Content            │
│         (100% width)            │
│                                 │
│                                 │
├─────────────────────────────────┤
│         BottomNav (56px)        │
└─────────────────────────────────┘
```

#### Tablet Layout (768-1279px)

```
┌─────────────────────────────────────────────┐
│                Header (64px)                │
├──────────┬──────────────────────────────────┤
│          │                                  │
│  SideNav │         Main Content             │
│  (200px) │         (calc(100% - 200px))     │
│          │                                  │
│          │                                  │
└──────────┴──────────────────────────────────┘
```

#### Desktop Layout (1280px+)

```
┌─────────────────────────────────────────────────────────────┐
│                      Header (64px)                          │
├──────────┬─────────────────────────────┬────────────────────┤
│          │                             │                    │
│  SideNav │       Main Content          │   Side Panel       │
│  (240px) │       (flex: 1)             │   (320px, opt)     │
│          │                             │                    │
│          │                             │                    │
└──────────┴─────────────────────────────┴────────────────────┘
```

### 4.2 챌린지 탭 레이아웃

```tsx
// 피드/장부/투표/모임/멤버 탭 공통 레이아웃
interface ChallengeTabLayoutProps {
  header?: React.ReactNode;      // EventCalendar 등
  sidebar?: React.ReactNode;     // MiniCalendar 등 (768px+)
  children: React.ReactNode;
}
```

#### Mobile (< 768px)

```
┌─────────────────────────────────┐
│  📚 책벌레들          [설정]   │
├─────────────────────────────────┤
│  [피드] [장부] [투표] [모임]   │
├─────────────────────────────────┤
│  Header (EventCalendar 등)     │
├─────────────────────────────────┤
│                                 │
│         Tab Content             │
│         (스크롤)                │
│                                 │
└─────────────────────────────────┘
```

#### Desktop (1280px+)

```
┌─────────────────────────────────────────────────────────────┐
│  📚 책벌레들                                    [설정] [알림] │
├─────────────────────────────────────────────────────────────┤
│  [피드] [장부] [투표] [모임] [멤버]                          │
├─────────────────────────────────────────┬───────────────────┤
│                                         │                   │
│  Header (EventCalendar 등)              │   MiniCalendar    │
│                                         │   (사이드바)       │
├─────────────────────────────────────────┤                   │
│                                         │   Quick Actions   │
│         Tab Content                     │                   │
│         (스크롤)                        │                   │
│                                         │                   │
└─────────────────────────────────────────┴───────────────────┘
```

---

## 5. 컴포넌트별 반응형 동작

### 5.1 네비게이션

| 컴포넌트 | Mobile | Tablet+ |
|---------|--------|---------|
| **BottomNav** | 표시 (56px 고정) | 숨김 |
| **SideNav** | 숨김 | 표시 (200-240px) |
| **Header** | 단순 (로고+햄버거) | 확장 (메뉴+잔액) |

### 5.2 오버레이

| 컴포넌트 | Mobile | Tablet+ |
|---------|--------|---------|
| **Modal** | 풀스크린 (100vh) | 중앙 다이얼로그 |
| **BottomSheet** | 표시 (vaul) | Modal로 대체 |
| **ActionSheet** | BottomSheet | Dropdown |

### 5.3 그리드 레이아웃

| 컴포넌트 | Mobile | Tablet | Desktop |
|---------|--------|--------|---------|
| **GroupCard** | 1열 (세로 스택) | 2열 그리드 | 3열 그리드 |
| **PostCard** | 100% 너비 | 100% 너비 | max-width: 680px |
| **MeetingCard** | 100% 너비 | 2열 그리드 | 2열 그리드 |

### 5.4 캘린더

| 컴포넌트 | Mobile | Tablet+ |
|---------|--------|---------|
| **EventCalendar** | 컴팩트 뷰 (가로 스크롤) | 풀 월 뷰 |
| **MiniCalendar** | 숨김 → DatePicker 대체 | 사이드바 표시 |
| **DatePicker** | 풀스크린 모달 | 드롭다운 |

---

## 6. 반응형 유틸리티 훅

```tsx
// hooks/useResponsive.ts
import { useMediaQuery } from '@/hooks/useMediaQuery';

export function useResponsive() {
  const isMobile = useMediaQuery('(max-width: 767px)');
  const isTablet = useMediaQuery('(min-width: 768px) and (max-width: 1279px)');
  const isDesktop = useMediaQuery('(min-width: 1280px)');
  const isWide = useMediaQuery('(min-width: 1920px)');
  
  return {
    isMobile,
    isTablet,
    isDesktop,
    isWide,
    // 편의 메서드
    isTabletOrAbove: isTablet || isDesktop || isWide,
    isDesktopOrAbove: isDesktop || isWide,
  };
}

// 사용 예
function ChallengeTabs() {
  const { isMobile, isDesktopOrAbove } = useResponsive();
  
  return (
    <div className="challenge-layout">
      <TabContent />
      {isDesktopOrAbove && <MiniCalendar />}
    </div>
  );
}
```

---

## 관련 문서

- [DESIGN_TOKENS.md](./DESIGN_TOKENS.md) - 메인 디자인 토큰
- [WDS_NAVIGATION.md](./WDS_NAVIGATION.md) - 네비게이션 컴포넌트
- [WDS_CALENDAR.md](./WDS_CALENDAR.md) - 캘린더 컴포넌트
- [FRONTEND_TECH_STACK.md](../FRONTEND_TECH_STACK.md) - 프론트엔드 기술 스택
